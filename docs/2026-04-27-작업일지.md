# 작업일지 — 2026-04-27

**프로젝트:** ㈜피플인피플 랜딩페이지 (pip-biz.com)
**저장소:** https://github.com/pinp10000-v1/pipin-homepage
**배포:** https://www.pip-biz.com

---

## 완료 작업 요약

### 1. P1.5 접근성 개선 (WCAG 2.2)

#### Critical 5건 (A1~A5)
| ID | 파일 | 내용 |
|----|------|------|
| A1 | `components/Projects.tsx` | 포트폴리오 이미지 CSS background → `<Image alt={project.name}>` 전환 |
| A2 | `components/Projects.tsx` | 캐러셀 버튼 `aria-label="이전/다음 포트폴리오"` 추가 |
| A3 | `components/Contact.tsx` | 문의 성공 메시지 `aria-live="polite" aria-atomic="true"` 추가 |
| A4 | `components/Navigation.tsx` | 모바일 메뉴 `aria-hidden={!menuOpen}` 토글 추가 |
| A5 | `components/Navigation.tsx` | 현재 페이지 `aria-current="page"` 추가 |

#### High 5건 (A6~A10)
| ID | 파일 | 내용 |
|----|------|------|
| A6 | `components/About.tsx` | CountUp 숫자 `aria-hidden="true"` + `sr-only` 최종값 노출 |
| A7 | `components/Solution.tsx`, `Services.tsx` | 장식용 대형 숫자 `aria-hidden="true"` |
| A8 | `Solution.tsx`, `Services.tsx`, `Contact.tsx`, `Projects.tsx` | 장식용 SVG 아이콘 전체 `aria-hidden="true"` |
| A9 | `app/layout.tsx` | 스킵 내비게이션 `<a href="#hero">본문으로 바로가기</a>` 추가 |
| A10 | `components/Hero.tsx` | 스크롤 인디케이터 `aria-hidden="true"` |

---

### 2. 신규 기능 (P0~P2)

#### P0 — 핵심 기능
- **Contact API** (`app/api/contact/route.ts`): Supabase 저장 + Gmail 이메일 전송
- **뉴스·인사이트 섹션** (`components/NewsInsight.tsx`): 신규 섹션 추가
- **뉴스 API** (`app/api/articles/route.js`, `app/api/articles/[slug]/route.js`): 목록·상세 라우트

#### P1 — 품질 개선
- Navigation 모바일 메뉴 UX 개선
- Projects 포트폴리오 이미지 `next/image` 전환

#### P2 — 성능·분석
- **Vercel SpeedInsights** 적용 (`app/layout.tsx`)
- `package.json` 패키지 추가: `@supabase/supabase-js ^2.49.1`, `@vercel/speed-insights ^1.1.0`

---

### 3. GitHub 커밋

```
커밋 해시: 4c0846d
브랜치: master
메시지: feat: P0~P2 + P1.5 접근성(WCAG 2.2) + SpeedInsights 통합 업데이트
변경 파일: 14개 (330 insertions, 159 deletions)
```

---

### 4. 도메인 연결 (pip-biz.com)

#### Namecheap DNS 설정
| Type | Host | Value |
|------|------|-------|
| A Record | @ | 216.198.79.1 |
| CNAME Record | www | 0c3b386700c87f7b.vercel-dns-017.com |

#### Vercel 도메인 등록
| 도메인 | 상태 |
|--------|------|
| pip-biz.com | ✅ Valid Configuration (307 → www) |
| www.pip-biz.com | ✅ Valid Configuration (Production) |
| pipin-homepage.vercel.app | ✅ Valid Configuration (Production) |

#### DNS 전파 상황
- Google DNS (8.8.8.8): ✅ 정상 (`216.198.79.1`)
- ISP/공유기 DNS: ⏳ 전파 중 (2026-04-27 기준, 2~6시간 소요 예상)
- Windows DNS 임시 설정: `8.8.8.8` / `8.8.4.4` (Google DNS)

---

---

## 세션 2 — 22:32 추가 작업

### SMTP_PASS 환경변수 추가 및 재배포

| 항목 | 내용 |
|------|------|
| 작업 | Gmail 앱 비밀번호 → Vercel 환경변수 `SMTP_PASS` 추가 |
| 명령 | `vercel env add SMTP_PASS production` |
| 재배포 | `vercel --prod --yes` → 빌드 성공 |
| 결과 | 문의 폼 이메일 전송 기능 정상 활성화 |
| 수신 메일 | `pinp10000@gmail.com` |

**배포 URL:** https://www.pip-biz.com ✅

---

## 잔여 과제

| 우선순위 | 항목 |
|---------|------|
| P1.5 Medium | A11: Contact 에러 `alert()` → `role="alert"` 인라인 처리 |
| P1.5 Medium | A12: Services/Projects/NewsInsight `<div>` → `<article>` 전환 |
| P2 | 뉴스 자동화 파이프라인 (n8n + GitHub Actions) |
| P2 | 청라 SK V1 서브페이지 |
| P3 | 성능: googleapis 번들 최적화 |
| P4 | SEO: sitemap.xml, robots.txt, JSON-LD |
| P4 | 뉴스 상세 페이지 (`/news/[slug]`) |
